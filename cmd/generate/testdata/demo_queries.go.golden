// Code generated by go-bindings-wmi-codegen. DO NOT EDIT.

//go:build windows

package demo

import wmi "github.com/deploymenttheory/go-bindings-wmi/runtime/wmi"

// QueryDemoBinding runs the WQL query against the class and decodes each
// instance into a DemoBinding. Pass the WHERE clause (or "" for all);
// build safe clauses with wmi.Where.
func QueryDemoBinding(svc *wmi.Service, where string) ([]DemoBinding, error) {
	q := "SELECT * FROM Demo_Binding"
	if where != "" {
		q += " WHERE " + where
	}
	rows, err := svc.Query(q)
	if err != nil {
		return nil, err
	}
	out := make([]DemoBinding, len(rows))
	for i, row := range rows {
		out[i] = DemoBindingFromRow(row)
	}
	return out, nil
}

// QueryOneDemoBinding returns the single Demo_Binding matching the WHERE
// clause ("" when one instance exists), or wmi.ErrNotFound when none match.
func QueryOneDemoBinding(svc *wmi.Service, where string) (*DemoBinding, error) {
	out, err := QueryDemoBinding(svc, where)
	if err != nil {
		return nil, err
	}
	if len(out) == 0 {
		return nil, wmi.ErrNotFound
	}
	return &out[0], nil
}

// GetDemoBinding returns the Demo_Binding instance identified by its key
// properties, or wmi.ErrNotFound.
func GetDemoBinding(svc *wmi.Service, antecedent string, index uint32) (*DemoBinding, error) {
	where := "Antecedent = " + wmi.WQLValue(antecedent) +
		" AND " + "Index = " + wmi.WQLValue(index)
	return QueryOneDemoBinding(svc, where)
}

// QueryDemoEmpty runs the WQL query against the class and decodes each
// instance into a DemoEmpty. Pass the WHERE clause (or "" for all);
// build safe clauses with wmi.Where.
func QueryDemoEmpty(svc *wmi.Service, where string) ([]DemoEmpty, error) {
	q := "SELECT * FROM Demo_Empty"
	if where != "" {
		q += " WHERE " + where
	}
	rows, err := svc.Query(q)
	if err != nil {
		return nil, err
	}
	out := make([]DemoEmpty, len(rows))
	for i, row := range rows {
		out[i] = DemoEmptyFromRow(row)
	}
	return out, nil
}

// QueryOneDemoEmpty returns the single Demo_Empty matching the WHERE
// clause ("" when one instance exists), or wmi.ErrNotFound when none match.
func QueryOneDemoEmpty(svc *wmi.Service, where string) (*DemoEmpty, error) {
	out, err := QueryDemoEmpty(svc, where)
	if err != nil {
		return nil, err
	}
	if len(out) == 0 {
		return nil, wmi.ErrNotFound
	}
	return &out[0], nil
}

// QueryDemoProfile runs the WQL query against the class and decodes each
// instance into a DemoProfile. Pass the WHERE clause (or "" for all);
// build safe clauses with wmi.Where.
func QueryDemoProfile(svc *wmi.Service, where string) ([]DemoProfile, error) {
	q := "SELECT * FROM Demo_Profile"
	if where != "" {
		q += " WHERE " + where
	}
	rows, err := svc.Query(q)
	if err != nil {
		return nil, err
	}
	out := make([]DemoProfile, len(rows))
	for i, row := range rows {
		out[i] = DemoProfileFromRow(row)
	}
	return out, nil
}

// QueryOneDemoProfile returns the single Demo_Profile matching the WHERE
// clause ("" when one instance exists), or wmi.ErrNotFound when none match.
func QueryOneDemoProfile(svc *wmi.Service, where string) (*DemoProfile, error) {
	out, err := QueryDemoProfile(svc, where)
	if err != nil {
		return nil, err
	}
	if len(out) == 0 {
		return nil, wmi.ErrNotFound
	}
	return &out[0], nil
}

// GetDemoProfile returns the Demo_Profile instance identified by its key
// properties, or wmi.ErrNotFound.
func GetDemoProfile(svc *wmi.Service, kind DemoProfileKind) (*DemoProfile, error) {
	where := "Kind = " + wmi.WQLValue(kind)
	return QueryOneDemoProfile(svc, where)
}

// QueryDemoWidget runs the WQL query against the class and decodes each
// instance into a DemoWidget. Pass the WHERE clause (or "" for all);
// build safe clauses with wmi.Where.
func QueryDemoWidget(svc *wmi.Service, where string) ([]DemoWidget, error) {
	q := "SELECT * FROM Demo_Widget"
	if where != "" {
		q += " WHERE " + where
	}
	rows, err := svc.Query(q)
	if err != nil {
		return nil, err
	}
	out := make([]DemoWidget, len(rows))
	for i, row := range rows {
		out[i] = DemoWidgetFromRow(row)
	}
	return out, nil
}

// QueryOneDemoWidget returns the single Demo_Widget matching the WHERE
// clause ("" when one instance exists), or wmi.ErrNotFound when none match.
func QueryOneDemoWidget(svc *wmi.Service, where string) (*DemoWidget, error) {
	out, err := QueryDemoWidget(svc, where)
	if err != nil {
		return nil, err
	}
	if len(out) == 0 {
		return nil, wmi.ErrNotFound
	}
	return &out[0], nil
}

// GetDemoWidget returns the Demo_Widget instance identified by its key
// properties, or wmi.ErrNotFound.
func GetDemoWidget(svc *wmi.Service, name string) (*DemoWidget, error) {
	where := "Name = " + wmi.WQLValue(name)
	return QueryOneDemoWidget(svc, where)
}

// QueryDemoWidgetMode runs the WQL query against the class and decodes each
// instance into a DemoWidgetMode. Pass the WHERE clause (or "" for all);
// build safe clauses with wmi.Where.
func QueryDemoWidgetMode(svc *wmi.Service, where string) ([]DemoWidgetMode, error) {
	q := "SELECT * FROM Demo_WidgetMode"
	if where != "" {
		q += " WHERE " + where
	}
	rows, err := svc.Query(q)
	if err != nil {
		return nil, err
	}
	out := make([]DemoWidgetMode, len(rows))
	for i, row := range rows {
		out[i] = DemoWidgetModeFromRow(row)
	}
	return out, nil
}

// QueryOneDemoWidgetMode returns the single Demo_WidgetMode matching the WHERE
// clause ("" when one instance exists), or wmi.ErrNotFound when none match.
func QueryOneDemoWidgetMode(svc *wmi.Service, where string) (*DemoWidgetMode, error) {
	out, err := QueryDemoWidgetMode(svc, where)
	if err != nil {
		return nil, err
	}
	if len(out) == 0 {
		return nil, wmi.ErrNotFound
	}
	return &out[0], nil
}
